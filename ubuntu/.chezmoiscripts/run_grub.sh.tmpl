#!/bin/bash
set -o errexit
set -o nounset
set -o pipefail

{{ template "init.sh" }}

tmp_dir="$(mktemp --directory)"

run git clone --depth 1 https://github.com/vinceliuice/grub2-themes.git "${tmp_dir}"
cd "${tmp_dir}"
sed --in-place "s/clear/:/g" install.sh
run sudo ./install.sh --screen 4k
run rm --force --recursive "${tmp_dir}"
