#!/usr/bin/bash
# set -o errexit
set -o nounset
set -o pipefail

{{ template "init.sh" }}

{{ template "load-pnpm.sh" }}

packages=(
  @bitwarden/cli
  commitizen
  serve
  vercel
)

for pkg in "${packages[@]}"; do
  run-safe pnpm add --global "${pkg}@latest"
done
