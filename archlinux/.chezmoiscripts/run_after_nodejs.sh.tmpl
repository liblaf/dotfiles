#!/usr/bin/bash
set -o errexit
set -o nounset
set -o pipefail

{{ template "init.sh" }}

packages=(
  @bitwarden/cli
  commitizen
  serve
  vercel
)

set +o errexit
for pkg in "${packages[@]}"; do
  run pnpm install --global "${pkg}@latest"
done

exit 0
