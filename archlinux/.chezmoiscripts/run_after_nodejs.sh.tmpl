#!/usr/bin/bash
set -o errexit
set -o nounset
set -o pipefail

{{ template "init.sh" }}

{{ template "load-pnpm.sh" }}

packages=(
  @bitwarden/cli
  commitizen
  serve
  vercel
)

set +o errexit
for pkg in "${packages[@]}"; do
  run pnpm add --global "${pkg}@latest"
done

exit 0
