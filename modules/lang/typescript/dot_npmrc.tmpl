# -*- mode: ini; -*-
# ref: <https://pnpm.io/npmrc>

# Registry & Authentication Settings
# ! `chsrc measure npm`
# ! FASTEST mirror site: Upstream
# registry = {{ .mirrors.npm }}/

# Node.js Settings
# node-mirror:release = {{ .mirrors.nodejs_release }}
