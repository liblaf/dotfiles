#!/bin/bash
# -*- mode: sh; -*-
set -o errexit
set -o nounset
set -o pipefail

# {{ if eq .chezmoi.hostname "PC07" }}
sudo systemctl enable --now "$(systemd-escape --suffix='automount' --path '/mnt/PC07sda1')"
sudo systemctl enable --now xfs_scrub_all.timer
# {{ end }}
