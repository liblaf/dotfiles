#!/bin/bash
# -*- mode: sh; -*-
set -o errexit
set -o nounset
set -o pipefail

# {{ includeTemplate "docker.sh" }}

# {{ if .services.caddy.enable }}

docker compose up --detach --remove-orphans

# {{ else }}

docker compose down

# {{ end }}
