#!/bin/bash
# -*- mode: sh; -*-
set -o errexit
set -o nounset
set -o pipefail

# {{ includeTemplate "docker.sh" }}
# {{ includeTemplate "service.sh" . }}

# {{ if .services.qbittorrent.enable }}

service-user-add qbittorrent '{{ .services.qbittorrent.uid }}' 'qBittorrent'
mkdir --parents --verbose ./config
mkdir --parents --verbose /mnt/PC06sda1/srv/qbittorrent/downloads
sudo chown --changes --recursive qbittorrent:srv ./config
sudo chown --changes --recursive qbittorrent:srv /mnt/PC06sda1/srv/qbittorrent/downloads
docker compose up --detach --remove-orphans

# {{ else }}

docker compose down

# {{ end }}
