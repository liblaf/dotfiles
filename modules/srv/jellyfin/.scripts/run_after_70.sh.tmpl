#!/bin/bash
# -*- mode: sh; -*-
set -o errexit
set -o nounset
set -o pipefail

# {{ template "docker.sh" }}

cd "$HOME/srv/jellyfin"

# {{ if .services.jellyfin }}

mkdir --parents --verbose "$PWD/config"
sudo chown --verbose '{{ .services.uid }}:{{ .services.gid }}' "$PWD/config"
docker compose up --detach --remove-orphans

# {{ else }}

docker compose down

# {{ end }}
