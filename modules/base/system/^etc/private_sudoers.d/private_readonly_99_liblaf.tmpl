# -*- mode: ini; -*-

Defaults env_keep += "EDITOR PAGER"

# ref: <https://wiki.archlinux.org/title/Proxy_server#Keep_proxy_through_sudo>
Defaults env_keep += "*_proxy *_PROXY"

# ref: <file:///etc/sudoers>
{{ .chezmoi.username }} ALL=(ALL:ALL) NOPASSWD: ALL
