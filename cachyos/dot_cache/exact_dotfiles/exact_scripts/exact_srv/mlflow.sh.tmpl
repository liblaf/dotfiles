#!/bin/bash
# shellcheck disable=SC2317
set -o errexit
set -o nounset
set -o pipefail

cd "$HOME/srv/mlflow"

# {{- if not .service.mlflow }}
docker compose down
exit
# {{- end }}

docker compose up --detach --pull always
