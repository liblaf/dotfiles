# -*- mode: ini; -*-

# Use 'systemd-analyze cat-config systemd/resolved.conf' to display the full config.
#
# See resolved.conf(5) for details.

[Resolve]
# "allow-downgrade" does not work for me.
DNSSEC = no
