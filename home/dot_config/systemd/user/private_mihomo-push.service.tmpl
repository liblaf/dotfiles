# -*- mode: ini; -*-

[Unit]
Description = Push Mihomo Configuration

[Service]
Environment = CLOUDFLARE_ACCOUNT_ID='{{ (rbwFields "Cloudflare").ACCOUNT_ID.value }}'
Environment = CLOUDFLARE_API_TOKEN='{{ (rbwFields "sub-store").CLOUDFLARE_API_TOKEN.value }}'
Environment = REMOTE=true
ExecStartPre = bun install
ExecStart = mise run build
Restart = on-failure
RestartSec = 1 hour
WorkingDirectory = %h/github/liblaf/sub-store/
