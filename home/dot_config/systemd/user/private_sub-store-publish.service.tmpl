# -*- mode: ini; -*-

[Unit]
Description = Publish Sub-Store Configuration

[Service]
Environment = SUB_STORE_API_TOKEN='{{ (rbwFields "sub-store").API_TOKEN.value }}'
ExecSearchPath = %h/.bun/bin:/usr/local/bin:/usr/bin
ExecStart = sub-store publish
Restart = on-failure
RestartSec = 1 hour
