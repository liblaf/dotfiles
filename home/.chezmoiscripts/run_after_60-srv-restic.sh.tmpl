#!/bin/bash
set -o errexit
set -o nounset
set -o pipefail

# {{ template "docker.sh" }}

# {{ if eq .virt "none" }}

cd "$HOME/srv/restic"
docker compose up --remove-orphans --wait

# {{ else }}

docker compose down

# {{ end }}
