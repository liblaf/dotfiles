#!/usr/bin/zsh
# set -o errexit
set -o nounset
set -o pipefail

{{ template "init.sh" }}
{{ template "load-pnpm.sh" }}

if has bw; then
  mkdir --parents --verbose "${HOME}/.zfunc"
  bw completion --shell zsh > "${HOME}/.zfunc/_bw"
fi
